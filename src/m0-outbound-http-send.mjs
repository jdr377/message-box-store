/** Compatibility path: M0 proofs exercise the production outbound runtime. */
export {
  createMessageBoxHttpSendCapability,
  OUTBOUND_SEND_STATES,
  sendPreparedHttpOnce,
} from './outbound-runtime.js'
