/** Compatibility path: M0 proofs exercise the production envelope runtime. */
export {
  bodyHash,
  canonicalRecordKey,
  decryptArchivedBody,
  extractEncryptedMessage,
  MESSAGEBOX_KEY_ID,
  MESSAGEBOX_PROTOCOL,
  plaintextText,
  prepareEncryptedBody,
} from './envelope-runtime.js'
