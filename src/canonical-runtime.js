// Shared runtime entry point. The implementation is kept in canonical.js so
// TypeScript's same-basename resolver cannot mistake the typed facade for its
// own runtime module.
export * from './canonical.js'
